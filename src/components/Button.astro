---
const { variant = "primary" } = Astro.props;
---

<button class={variant}>
    {(<slot />)}
</button>

<style>
    button {
        background-color: rgba(228, 30, 40, 1);
        border-radius: 30px;
        padding: 15px 20px;
        cursor: pointer;
    }


</style>
